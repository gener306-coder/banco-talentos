# Credenciales exclusivas del entorno local.
POSTGRES_DB=banco_talentos
POSTGRES_USER=banco_talentos
POSTGRES_PASSWORD=local_password

DB_PORT=5432
BACKEND_PORT=8000
FRONTEND_PORT=5173

# En Linux, usar los valores de `id -u` e `id -g`.
LOCAL_UID=1000
LOCAL_GID=1000
